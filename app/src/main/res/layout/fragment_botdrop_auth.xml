<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:background="@color/botdrop_background">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp">

        <!-- Header -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="← Step 2/4"
            android:textSize="14sp"
            android:textColor="@color/botdrop_secondary_text"
            android:layout_marginBottom="16dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Choose your AI"
            android:textSize="24sp"
            android:textStyle="bold"
            android:textColor="@color/botdrop_on_background"
            android:layout_marginBottom="8dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Select an AI model to power your bot"
            android:textSize="14sp"
            android:textColor="@color/botdrop_secondary_text"
            android:layout_marginBottom="32dp" />

        <!-- Model Selection -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="AI Model"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="@color/botdrop_on_background"
            android:layout_marginBottom="12dp" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="32dp">

            <EditText
                android:id="@+id/auth_model_text"
                android:layout_width="0dp"
                android:layout_height="48dp"
                android:layout_weight="1"
                android:hint="No model selected"
                android:textSize="12sp"
                android:textColor="@color/botdrop_on_background"
                android:textColorHint="@color/botdrop_secondary_text"
                android:background="@drawable/edit_text_background"
                android:paddingStart="12dp"
                android:paddingEnd="12dp"
                android:fontFamily="monospace"
                android:focusable="false"
                android:clickable="false"
                android:cursorVisible="false"
                android:layout_marginEnd="12dp" />

            <Button
                android:id="@+id/auth_select_button"
                android:layout_width="wrap_content"
                android:layout_height="48dp"
                android:text="Select"
                android:background="@drawable/botdrop_button_bg"
                android:textColor="@color/botdrop_background"
                android:paddingStart="24dp"
                android:paddingEnd="24dp" />

        </LinearLayout>

    </LinearLayout>

</ScrollView>
